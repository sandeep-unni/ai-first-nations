{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "startCommand": "gunicorn --chdir flask-application app:app --workers 1 --worker-class gthread --threads 2 --timeout 300"
  }
}
